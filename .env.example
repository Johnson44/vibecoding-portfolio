# Optional CloudBase demo configuration for the lead-material-acquisition app.
# Keep credentials out of Git and inject them only in the deployment environment.
VITE_API_MODE=demo
VITE_CLOUDBASE_ENV_ID=
VITE_CLOUDBASE_FUNCTION_NAME=aigc-api
VITE_CLOUDBASE_ACCESS_KEY=

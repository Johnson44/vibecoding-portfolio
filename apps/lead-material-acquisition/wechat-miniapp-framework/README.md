# 新福利礼包小程序框架

这个目录是新的原生微信小程序框架，与原资料库小程序分开保存。

页面只由两张图片构成：

- 主图：覆盖右下角方块以外的全部区域；
- 右下角图片：独立图片位，固定为 1:1 正方形。

当前主图已配置为 `images/teacher-qr-poster.jpg`，右下角图片位默认留空。管理员页目前保存到本机缓存，正式版本再接入云存储和管理员权限。

## 导入

在微信开发者工具中导入当前 `wechat-miniapp-framework` 目录。
